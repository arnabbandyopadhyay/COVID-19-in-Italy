### Testing model

This folder contains:

- Models folder (contains .def files with state variable and equations)
- fixed_undetected.m ; fixloop_undet.m (matlab files to generate the results)
- additional matlab files: Rocalc_IXD.m ; xlwrite.m ; Func_replace_string.m
- ref_results folder (contains .mat files storing the parameters resulting from the Ref. model fit)

###

To generate Fig 4a and 4b:

1) change the relevant paths in 'fixed_undetected.m' and 'fixloop_undet.m'

2) adjust "day_test" variable in 'fixloop_undet.m' (day_test = 7; 14; 21)

3) run 'fixed_undetected.m'

4) for day_test = 7; 14; 21, move the results in folder (res8; res15; res22) respctively

5) run increase_test.R

###
